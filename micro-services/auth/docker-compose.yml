services:
  auth-api:
    build:
      context: .
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "5001:5000"

  auth-tests:
    build:
      context: .
    env_file:
      - .env
    command: ["pytest", "tests", "-v"]
    profiles:
      - test
