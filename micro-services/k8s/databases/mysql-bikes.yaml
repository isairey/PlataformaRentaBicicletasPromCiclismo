apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-bikes
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-bikes
  template:
    metadata:
      labels:
        app: mysql-bikes
    spec:
      containers:
        - name: mysql-bikes
          image: mysql:8.0
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: "rootpass"
            - name: MYSQL_DATABASE
              value: "bikes_db"
            - name: MYSQL_USER
              value: "bikes_user"
            - name: MYSQL_PASSWORD
              value: "bikes_pass"
          ports:
            - containerPort: 3306
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-bikes
spec:
  selector:
    app: mysql-bikes
  ports:
    - port: 3306
      targetPort: 3306
